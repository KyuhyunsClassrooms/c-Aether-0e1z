# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)

(예시 - 관리 프로그램의 경우)
```
시작
    count ← 0        // 현재 등록된 기차 수 초기화

    반복 (사용자가 '4. 종료'를 선택할 때까지):
        메뉴 출력:
            1. 기차 정보 등록
            2. 기차 정보 조회
            3. 전체 기차 목록 보기
            4. 종료

        사용자로부터 메뉴 선택 입력받기 → menu

        만약 menu == 1 이면:
            기차정보등록() 함수 호출
        만약 menu == 2 이면:
            기차정보조회() 함수 호출
        만약 menu == 3 이면:
            전체기차목록보기() 함수 호출
        만약 menu == 4 이면:
            "프로그램을 종료합니다." 출력
            반복 종료
        그 외의 경우:
            "잘못된 입력입니다." 출력
    반복 끝
종료
2. 핵심 기능 1: 기차 정보 등록 함수 (기차정보등록())
markdown
코드 복사
시작
    만약 count >= 최대 저장 개수(예: 10) 이면:
        "더 이상 등록할 수 없습니다." 출력
        함수 종료

    기차 번호 입력받기 → trains[count].trainNo
    출발역 입력받기 → trains[count].startStation
    도착역 입력받기 → trains[count].endStation
    출발시간 입력받기 → trains[count].startTime
    도착시간 입력받기 → trains[count].endTime

    count ← count + 1
    "기차 정보 등록이 완료되었습니다." 출력
종료
```

## 2. 핵심 기능 1:
```
(핵심 기능 1의 논리적 절차를 작성하세요.)

예시:
시작
    만약 count >= 최대 저장 개수(예: 10) 이면:
        "더 이상 등록할 수 없습니다." 출력
        함수 종료

    기차 번호 입력받기 → trains[count].trainNo
    출발역 입력받기 → trains[count].startStation
    도착역 입력받기 → trains[count].endStation
    출발시간 입력받기 → trains[count].startTime
    도착시간 입력받기 → trains[count].endTime

    count ← count + 1
    "기차 정보 등록이 완료되었습니다." 출력
종료
```

## 3. 핵심 기능 2: 
```
(핵심 기능 2의 논리적 절차를 작성하세요.)
```
시작
    조회할 기차번호 입력받기 → searchNo
    found ← 0   // 검색 성공 여부 표시 변수

    i ← 0 부터 count-1 까지 반복:
        만약 trains[i].trainNo == searchNo 이면:
            해당 기차의 출발역, 도착역, 출발/도착시간 출력
            found ← 1
            반복 종료
    반복 끝

    만약 found == 0 이면:
        "해당 기차 정보를 찾을 수 없습니다." 출력
종료
